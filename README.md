# ArgoCD
This repository implements a GitOps continuous delivery pipeline using ArgoCD to deploy a Helm chart on Kubernetes. It features automated synchronization, self-healing, and live drift detection directly triggered by Git updates.
